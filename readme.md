# BGSCET_CB076

This repository is for the BGSCET_CB076 project.

## Development

Currently working on the `dev-alwin` branch. The `main` branch is for stable production releases. 

### Instructions
- Use `git checkout -b <branch-name>` to create new features.
- Merge back into `main` only after full review.